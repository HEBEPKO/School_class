services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: school_class
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: neverko
    ports:
      - "5432:5432"
    volumes:
      - gdata:/var/lib/postgresql/data

